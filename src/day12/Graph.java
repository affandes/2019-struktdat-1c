package day12;


public class Graph {
    public Node[] v; // [a,b,c,d,e]
    public boolean[][] e;

    public Edge[] getMst() {
        boolean[] isConnected = new boolean[7];
        // LinkedList pilihan;
        Edge[] mst = new Edge[v.length-1];

        // Cari jalur awal, inputkan ke mst

        // Tandai titik awal

        //while (false) {

            // Inputkan pilihan

            // Eliminasi yang sudah terkoneksi

            // Pilih yang paling kecil, inputkan ke mst

            // Tandai yang sudah terkoneksi
        //}

        return mst;
    }

}
